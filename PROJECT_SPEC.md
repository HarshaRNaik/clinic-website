# Clinic Portal — Project specification

This repository is being built in phases to match the security-first clinic portal specification supplied by the product brief. This file is intentionally kept as a working reference so the implementation stays aligned with the stated clinic requirements, patient privacy controls, and phased build order.

See the attached project brief for the full requirements and the execution order.
