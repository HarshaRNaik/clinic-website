rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Root deny-all default; every collection path must be opened explicitly.
    allow read, write: if false;
  }
}
